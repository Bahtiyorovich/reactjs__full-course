import {Button} from './'

const UploadButton = () => {

  return (
    <div>
        <Button onClick={() => alert('Uploading filename')}>Uploading</Button>
    </div>
  )
}

export default UploadButton