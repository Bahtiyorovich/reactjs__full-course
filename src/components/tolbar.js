import {UploadButton, PlayButton, SignUp} from './'

const Tolbar = () => {
  return (
    <div>
        <UploadButton/>
        <PlayButton movieName="Harry Poter"/>
        <SignUp/>
    </div>
  )
}

export default Tolbar